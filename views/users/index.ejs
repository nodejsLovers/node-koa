<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <% include ./../common/header.ejs %>
</head>
<body>
<h1><%= title %></h1>
<ul id="users">
    <% if (users.length) { %>
        <% users.forEach(function(user){ %>
        <li><%= user.name %></li>
        <% }) %>
    <% } %>
</ul>
</body>
</html>
